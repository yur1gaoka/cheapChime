# handover.md

## 目的
作業状態の維持
継続作業やスレッド分離時に必要な情報を引き継ぐ

---

## 現在状態
- 更新日時：2026-04-02 18:47:50 +09:00
- 作業者：Codex
- ブランチ/worktree：`main`
- フェーズ：docs テンプレート整備
- 完了：
  - `docs/project-profile.md` を現行リポジトリの事実ベースで記入
  - `docs/handover.md` を現行状態向けに初期化
  - ブラウザ単体構成、非 Docker / 非 DB 前提を文書化
- 未完了：
  - 今後の個別作業ごとの `tasks.md` 準拠タスク定義
  - 継続開発時の handover 更新
  - 必要に応じた README / HANDOFF.md との内容同期

## 作業範囲
- やる：
  - `docs` テンプレートをこのリポジトリの現状に合わせて整備する
  - 以後の着手判断に必要な最低限の前提を文書化する
- やらない：
  - `index.html` の機能追加や音源変更
  - UI 調整
  - 未確認情報の断定記入

## 対象
- ファイル：
  - `docs/project-profile.md`
  - `docs/handover.md`
- 範囲：
  - 現行リポジトリから確認できる事実の記入
  - 運用開始に必要な初期状態の明文化

## 条件
- 技術：
  - HTML / CSS / JavaScript / Web Audio API / OfflineAudioContext
  - 単一 HTML ファイルをブラウザで直接開いて動作
- Docker：
  - 該当なし
- DB：
  - 該当なし

## 確認結果
- 実施した確認：
  - `README.md` でブラウザ単体動作と主要機能を確認
  - `index.html` で 2点 / 3点 / 4点チャイム、複数プリセット、コード進行、ボイス数、EQ、ループ、WAV 書き出しを確認

- 未確認：
  - 対応ブラウザの正式サポート範囲
  - 実機スマホでの最新挙動
  - ライセンス運用の最終方針
- 懸念：
  - 既存の `HANDOFF.md` には別環境由来の古い内容が含まれており、今後は `docs/handover.md` を基準に寄せた方が安全
  - `README.md` と現行実装の機能差分があるため、後続で整合確認が必要

## 次タスク
- `tasks.md` 準拠で次の具体作業を定義する
- 必要なら `README.md` と `HANDOFF.md` を現行 `index.html` に合わせて整理する

---

## ルール

- 継続作業、保留作業、複数スレッド作業では更新する
- 単発で完結し引き継ぎ不要な作業では省略可
- 次スレッドは必ず参照する
